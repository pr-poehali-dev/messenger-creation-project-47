CREATE TABLE am_sessions (id BIGSERIAL PRIMARY KEY, user_id BIGINT NOT NULL, token VARCHAR(128) UNIQUE NOT NULL, created_at TIMESTAMPTZ DEFAULT NOW(), expires_at TIMESTAMPTZ DEFAULT NOW() + INTERVAL '30 days');